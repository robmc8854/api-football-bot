{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NPM_CONFIG_PRODUCTION=false npm ci --include=dev --no-audit --no-fund && npm run build"
  },
  "deploy": {
    "startCommand": "node server.js",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120
  }
}